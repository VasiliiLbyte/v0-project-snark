# SNARK — Карта состояния (июль 2026)

Снимок «Сделано» на 2026-07-15. Редко меняется; детали реализации — в коде и [`../LOG.md`](../LOG.md).
Горячее состояние — [`../PROGRESS.md`](../PROGRESS.md).

---

## Инфраструктура и архитектура

- Next.js 16 App Router: **33** защищённых страницы + login; `app/page.tsx` → redirect `/dashboard`
- `middleware.ts` — JWT-сессия, guard `/admin/**` для `admin` | `hr_manager`
- PostgreSQL + Drizzle: schema, **20** миграций (`0000`–`0019`), mock/real режим (`USE_MOCK_DB`)
- Docker Compose (local): PostgreSQL, MinIO, Redis
- S3/MinIO storage, audit logs, zod-валидация
- Prod portal: NSSM-сервис `snark-portal` на `192.168.1.236` (см. [`../DEPLOY.md`](../DEPLOY.md))

---

## Модули с UI + API

- **Дашборд**, новости (+ admin CRUD), документы, база знаний (+ admin)
- **Справочник** `/contacts`, оргструктура `/structure`, импорт 1С `/admin/structure-import`
- **Профиль** (avatar crop), календарь, «О компании»
- **Service Desk:** `/support`, `/admin/tickets`, `/admin/support-categories`
- **HR:** отпуска + admin-согласование
- **Задачи:** список + карточка `/tasks/[id]`, kanban, checklist, comments, attachments, chat задачи, `from-message`
- **Чат:** каналы, direct, mentions, reply, SSE `/api/chat/events`, department channels (migration `0019`)
- **Протоколы:** UI `/protocols` + proxy API + Python microservice + internal webhook sync → tasks
- **Уведомления:** schema + API + notification bell
- **Админ:** users, employees, departments, news, knowledge, vacations, tickets

---

## API

- **74** route handlers: auth, tasks (12), chat (7), protocols (4 + internal sync), employees, HR, content, admin

---

## Тесты и качество (portal)

- Vitest: **52** теста (CI portal job); 12+ test-файлов
- Scripts: `pnpm lint`, `typecheck`, `test`, `db:migrate`, `seed`
- CI: корневой `.github/workflows/ci.yml` — portal (typecheck/test/lint) + protocols (ruff/pytest), paths-filter (`dce5861`)

---

## Оркестратор (2026-07-15)

- Хаб `orchestrator_doc/`: ORCHESTRATOR, PROGRESS, LOG, SPEC, API_CONTRACT, DEPLOY, `60-orchestrator.mdc`
- Executor rules: `55-executor-portal.mdc`, `55-executor-protocols.mdc`
- Hot-state PROGRESS + archive (CORR-сессия 2026-07-15)

---

*При крупных изменениях архитектуры — новый файл `archive/STATE-YYYY-MM.md` или обновление этого снимка.*
